import React from 'react';
import PropTypes from "prop-types";

const Filters = (props) => {
    return (<div>
        <h2>{'filters..!!'}</h2>
       </div>);
   }
   Filters.propTypes = {
   }
   
   Filters.defaultProps = {
   };
   
   export default Filters;